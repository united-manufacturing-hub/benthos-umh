diff --git a/internal/impl/kafka/franz_reader_ordered.go b/internal/impl/kafka/franz_reader_ordered.go
index bc0ca0e..273de8f 100644
--- a/internal/impl/kafka/franz_reader_ordered.go
+++ b/internal/impl/kafka/franz_reader_ordered.go
@@ -17,6 +17,8 @@ package kafka
 import (
 	"context"
 	"errors"
+	"fmt"
+	"regexp"
 	"sync"
 	"sync/atomic"
 	"time"
@@ -37,6 +39,7 @@ const (
 	kroFieldPartitionBuffer       = "partition_buffer_bytes"
 	kroFieldTopicLagRefreshPeriod = "topic_lag_refresh_period"
 	kroFieldMaxYieldBatchBytes    = "max_yield_batch_bytes"
+	kroFieldKeyPattern            = "key_pattern"
 )
 
 // FranzReaderOrderedConfigFields returns config fields for customising the
@@ -65,6 +68,10 @@ func FranzReaderOrderedConfigFields() []*service.ConfigField {
 				"and for high-throughput scenarios they should be equal.").
 			Default("32KB").
 			Advanced(),
+		service.NewStringField(kroFieldKeyPattern).
+			Description("An optional RE2 regular expression matched against each record's raw key. When set, records whose key does not match are dropped before message construction (a downstream key filter would drop them anyway), so this skips their build and delivery cost. Records are still committed (offsets advance over dropped records), so this changes which records are delivered, not which offsets advance. Leave unset to disable (every record is delivered).").
+			Default("").
+			Advanced(),
 	}
 }
 
@@ -83,6 +90,7 @@ type FranzReaderOrdered struct {
 	readBackOff           backoff.BackOff
 	topicLagRefreshPeriod time.Duration
 	batchMaxSize          uint64
+	keyFilter             *regexp.Regexp
 
 	res     *service.Resources
 	log     *service.Logger
@@ -123,6 +131,12 @@ func NewFranzReaderOrderedFromConfig(conf *service.ParsedConfig, res *service.Re
 		return nil, err
 	}
 
+	if keyPattern, _ := conf.FieldString(kroFieldKeyPattern); keyPattern != "" {
+		if f.keyFilter, err = regexp.Compile(keyPattern); err != nil {
+			return nil, fmt.Errorf("compiling %v: %w", kroFieldKeyPattern, err)
+		}
+	}
+
 	return &f, nil
 }
 
@@ -137,24 +151,42 @@ type batchWithRecords struct {
 	size uint64
 }
 
-func recordsToBatch(records []*kgo.Record, consumerLag *ConsumerLag) (batch batchWithRecords) {
-	batch.b = make([]*messageWithRecord, len(records))
+func recordsToBatch(records []*kgo.Record, consumerLag *ConsumerLag, keyFilter *regexp.Regexp) (batch batchWithRecords) {
+	batch.b = make([]*messageWithRecord, 0, len(records))
 
+	lastIdx := len(records) - 1
 	for i, r := range records {
-		msg := FranzRecordToMessageV1(r)
-		if consumerLag != nil {
-			lag := consumerLag.Load(r.Topic, r.Partition)
-			msg.MetaSetMut("kafka_lag", lag)
+		size := uint64(len(r.Value) + len(r.Key))
+
+		matched := keyFilter == nil || keyFilter.Match(r.Key)
+
+		// Non-matching record: omit entirely (no build, no batch entry → also
+		// skips the per-message context wrap in pop()). Its offset still commits
+		// because the LAST record of every poll is always kept — as a
+		// no-metadata placeholder when it doesn't match — so the batch's final
+		// offset reaches the partition high-water mark and the commit advances
+		// over the skipped run. The placeholder rides in the same ack-gated batch
+		// as any matches, so a NACK still blocks the commit (ENG-5094). With
+		// keyFilter == nil every record matches → behaviour is unchanged.
+		if !matched && i != lastIdx {
+			r.Key = nil
+			r.Value = nil
+			continue
 		}
 
-		rmsg := &messageWithRecord{
-			m:    msg,
-			r:    r,
-			size: uint64(len(r.Value) + len(r.Key)),
+		var msg *service.Message
+		if matched {
+			msg = FranzRecordToMessageV1(r)
+			if consumerLag != nil {
+				lag := consumerLag.Load(r.Topic, r.Partition)
+				msg.MetaSetMut("kafka_lag", lag)
+			}
+		} else {
+			msg = service.NewMessage(nil)
 		}
 
-		batch.b[i] = rmsg
-		batch.size += rmsg.size
+		batch.b = append(batch.b, &messageWithRecord{m: msg, r: r, size: size})
+		batch.size += size
 
 		// The record lives on for checkpointing, but we don't need the contents
 		// going forward so discard these. This looked fine to me but could
@@ -599,7 +631,7 @@ func (f *FranzReaderOrdered) Connect(ctx context.Context) error {
 					return
 				}
 
-				batch := recordsToBatch(p.Records, consumerLag)
+				batch := recordsToBatch(p.Records, consumerLag, f.keyFilter)
 				if len(batch.b) == 0 {
 					return
 				}
